window.onload = () => {

    /* Aqui poderemos inserir nosso script JavaScript - a princípio não utilizaremos, mas deixaremos listado para mostrarmos uma estrutura básica de projeto */
    // Já deixamos essa função pronta e atrelada ao evento do tipo load relacionado à window. Ou seja, o que estiver aqui dentro só será lido após o carregamento da tela.

}